<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.etsmtl.ca.log530.snake">

    <application android:allowBackup="true" android:icon="@drawable/icons_icon"
        android:label="@string/app_name" android:roundIcon="@drawable/icons_icon"
        android:supportsRtl="true" android:theme="@style/AppTheme" android:isGame="true" android:name=".SnakeApplication">
        <activity android:name=".SnakeGameActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name" android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>